using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UIElements;

public class WorldController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // if mouse click, create a QuickScanner object
        //if (Input.GetMouseButtonDown(0))
        //{
        //    GameObject scanner = new GameObject();
        //    scanner.AddComponent<QuickScanner>();

        //}

        //if (Input.GetKey(KeyCode.K)) { 
        //    GameObject areaScanner = new GameObject();
        //    areaScanner.AddComponent<AreaScannerUIController>();
        //    areaScanner.name = "AreaScannerUIController";
            
        //}
          
    }
    



}
